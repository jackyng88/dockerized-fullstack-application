<!DOCTYPE html>
<html lang="en">

<head>

    <!-- Meta tags for character set, viewport, and compatibility -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <!-- Page title -->
    <title>Sample Application</title>

    <!-- Internal CSS styles -->
    <style>
        /* Styling for the body element */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f2f2f2;
        }

        /* Styling for the header element */
        header {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 20px 0;
        }

        /* Styling for the h1 element within the header */
        h1 {
            margin: 0;
        }

        /* Styling for the main content area */
        main {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Styling for forms */
        form {
            margin-bottom: 20px;
        }

        /* Styling for labels within forms */
        label {
            display: block;
            font-weight: bold;
        }

        /* Styling for text inputs and text areas */
        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        /* Styling for submit buttons */
        input[type="submit"] {
            background-color: #333;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        /* Styling for submit buttons on hover */
        input[type="submit"]:hover {
            background-color: #555;
        }

        /* Styling for h4 elements */
        /* Styling for h4 elements */
        h4 {
            margin-top: 20px;
        }

        /* Styling for unordered lists */
        ul {
            list-style-type: none;
            padding: 0;
        }

        /* Styling for list items */
        li {
            border: 1px solid #ccc;
            background-color: #fff;
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 4px;
        }
    </style>
</head>

<body>

    <!-- Header section with a blog title -->
    <header>
        <h1>Blog Homepage</h1>
    </header>

    <!-- Main content section -->
    <main>

        <!-- Form for adding a new blog post -->
        <form method="post" action="/item/add">
            <label for="name">Name</label>

            <!-- Input field for the author's name (required) -->
            <input type="text" name="name" required>
            <br>
            <label for="post">Post</label>

            <!-- Text area for the blog post content (required) -->
            <textarea name="post" rows="4" placeholder="What do you want to say?" required></textarea>
            <br>

            <!-- Submit button for adding a new post -->
            <input type="submit" value="Add Post">
        </form>

        <!-- Section for displaying all blog posts -->
        <h4>All Posts:</h4>
        <ul>

            <!-- Loop through items to display each blog post -->
            <% items.forEach(function(item) { %>
                <li>

                    <!-- Display the author's name -->
                    <h3>Post by - <%= item.name %>
                    </h3>

                    <!-- Display the blog post content -->
                    <p>
                        <%= item.post %>
                    </p>
                </li>
                <% }); %>
        </ul>
    </main>
</body>

</html>
